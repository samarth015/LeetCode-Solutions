#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool stoneGame(vector<int>& piles) {
		int alice{0}, bob{0};
		int i{0}, j{ (int)piles.size() };
		bool turn = true;
		while(i <= j){
			if(piles[i] > piles
			if(turn)
		}
    }
};

int main(){
}

